services:

  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm-proxy
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      # Must match volume mount — LiteLLM reads access-token + api-key.json here only.
      GITHUB_COPILOT_TOKEN_DIR: /root/.config/litellm/github_copilot
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./github_copilot:/root/.config/litellm/github_copilot
    command: ["--config", "/app/config.yaml", "--port", "4000", "--host", "0.0.0.0"]
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python3",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:4000/health/liveliness', timeout=4)",
        ]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 45s

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: litellm-tunnel
    command: tunnel --no-autoupdate --url http://litellm:4000
    depends_on:
      litellm:
        condition: service_healthy
    restart: unless-stopped
